## 实现计划

### 1. 项目初始化
- 创建项目目录结构
- 创建主程序入口 main.py

### 2. 网络模块 (network.py)
- 实现网卡信息获取（使用psutil获取所有网卡IP）
- 实现TCP客户端类（支持绑定指定源IP）
- 实现TCP服务器类（支持绑定指定IP监听）

### 3. GUI模块 (gui.py)
- 使用tkinter创建主窗口
- 左侧：网卡选择列表（显示网卡名+IP）
- 中间：连接配置区（目标IP、端口、连接按钮）
- 右侧：数据收发区（发送输入框、接收显示框、十六进制切换）

### 4. 工具模块 (utils.py)
- 数据格式转换（文本/十六进制互转）
- 日志记录功能

### 5. 主程序整合 (main.py)
- 整合所有模块
- 添加异常处理

### 技术要点
- 使用 `socket.bind((source_ip, 0))` 指定源IP
- 使用 `psutil.net_if_addrs()` 获取网卡信息
- 使用线程处理网络连接避免UI卡顿

请确认后我将开始编写代码。